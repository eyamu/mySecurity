<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bit15_21.mysecurity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Send SMS Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Phone  Call Permissions -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Location, GPS Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Audio Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mysecurity"
        android:label="@string/app_name" >
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IndexActivity"
            android:label="MySecurity" />
        <activity
            android:name=".NewContact"
            android:label="New Contact">
        </activity>        
        <activity
            android:name=".UserRegistrationActivity"
            android:label="@string/title_activity_user_registration" >
        </activity>
        <activity
            android:name=".DisplayContactsActivity"
            android:label="@string/title_activity_display_contacts_activity" >
        </activity>
        <activity
            android:name="com.bit15_21.record.CaptureAudioActivity"
            android:label="@string/title_activity_capture_audio" >
        </activity>
    </application>

</manifest>
